---
title: "Metadata for a specific asset"
description: "Retrieve metadata for a specific asset."
icon: "fish-bones"
---

```typescript
import { Mobula } from "mobula-api-sdk";

const mobula = new Mobula("YOUR_API_KEY_HERE");

mobula
  .fetchAssetMetadata({
    asset: "Bitcoin",
  })
  .then((response) => {
    console.log(response);
  })
  .catch((error) => {
    console.error("Error:", error);
  });
```

## Parameters

- `asset`: string
- `requestOptions`: RequestOptions
